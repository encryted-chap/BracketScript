

class BSCRIPT_TYPE {
public:
    size_t  Size,
            pDepth;     // pointer depth (**varname = 2)

    // parse a type from a plain
    // string declaration
    BSCRIPT_TYPE(char *vardef) {
        
    }
};